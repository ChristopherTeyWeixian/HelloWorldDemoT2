name: CI for Remote HelloWorld

on: 
  push:
    branches: [main]
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup Python 3.8 Test
      uses: actions/setup-python@v2
      with:
        python-version: 3.8
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest pytest-cov
    
    - name: Checkout Own Repo to cloud
      uses: actions/checkout@v2
      with: 
        repository: lowkh/HelloWorldDemoT02
    
    - name: Checkout private repo remotely
      uses: actions/checkout@v2
      with:
        repository: ChristopherTeyWeixian/12345
        path: tests
        token: ${{ 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 }}
        
      
    - name: Begin Pytest
      run: |
        ls
        mv ./tests/*.* ./
        pytest --cov
